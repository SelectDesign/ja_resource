name: Elixir CI

on:
  pull_request:
    types: [synchronize, opened, reopened, ready_for_review]

env:
  MIX_ENV: test
  CI: true

jobs:
  test:
    runs-on: ubuntu-24.04
    if: github.event.pull_request.draft == false

    steps:
      - uses: actions/checkout@v7
      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: "1.15.8" # Define the elixir version [required]
          otp-version: "25.3.2.15" # Define the OTP version [required]
      - name: Restore dependencies cache
        uses: actions/cache@v4
        with:
          path: deps
          key: ${{ runner.os }}-mix-${{ hashFiles('**/mix.lock') }}
          restore-keys: ${{ runner.os }}-mix-
      - name: Restore build cache
        uses: actions/cache@v4
        with:
          path: _build
          key: ${{ runner.os }}-mix-build-${{ hashFiles('**/mix.lock') }}
          restore-keys: ${{ runner.os }}-mix-build-
      - name: Clean dependencies that must recompile each build
        run: mix deps.clean ranch cowlib #Ranch and cowlib do not compile when cached so it must download every time as of Elixir 1.15
      - name: Install dependencies
        run: mix deps.get
      - name: Check format
        run: mix format --check-formatted
      - name: Compile dependencies
        run: DIAGNOSTIC=1 mix deps.compile --warnings-as-errors
      - name: Compile app
        run: mix compile
      - name: Run sobelow
        run: mix sobelow -v --skip --exit low
      - name: Run tests
        run: mix test
